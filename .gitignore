node_modules/
build/
dist/
coverage/
# Playwright E2E 产物
dist-e2e/
test-results/
playwright-report/
playwright/.cache/
.DS_Store
*.log
server.js
.env*
!.env.example
# 爬虫生成的数据快照（可重新抓取，不入库）
scraper/output/
.gstack/
# Claude agent 本地配置（保留 launch.json 入库，排除 local settings）
.claude/settings.local.json
# IDE
.idea/

# release 签名(只存本机,丢了以后更新包签名会变——记得备份)
android/release.keystore
android/keystore.properties

# iOS 构建产物(xcodebuild -derivedDataPath 的输出)。内含 SPM checkouts 等内嵌 git 仓库,
# 误提交会触发 "adding embedded git repository" 并把整个依赖树塞进仓库。
ios/App/DerivedData/
ios/App/DerivedDataDevice/
