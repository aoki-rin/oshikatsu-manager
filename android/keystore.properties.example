# Copy to keystore.properties. Never commit the real file or keystore.
# storeFile is relative to android/ (or an absolute path).
storeFile=release.keystore
storePassword=REPLACE_WITH_YOUR_STORE_PASSWORD
keyAlias=release
keyPassword=REPLACE_WITH_YOUR_KEY_PASSWORD
