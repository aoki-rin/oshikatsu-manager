# 可选：让所有票务平台优先经过你自己的代理服务。
# 默认留空：Android / iOS 直接访问票务平台，无需常驻电脑。
# Lawson：Android 使用 Cronet；iOS 使用 CapacitorHttp / URLSession。
# 浏览器 npm run dev 始终使用同源 /api，由 Vite 转发至 127.0.0.1:8787；
# 请另开终端运行 npm run server:dev。
#
# 自建代理请使用手机可访问的 HTTPS 地址。此值在构建时写入应用，
# 修改后需要重新 build + cap sync + 安装。不要在任何 VITE_ 变量中放密码。
# 代理请求失败时会尝试客户端直连；浏览器可能受目标网站 CORS 限制。
# npm run build:release 不加载 .env 文件，并强制清空代理地址。
VITE_TICKET_PROXY_BASE_URL=""
