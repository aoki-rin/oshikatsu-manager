# 票务代理（可选，且自 ADR-0005 起对 Android 已非必需）。
#
# ⚠️ Android 想让 Lawson 走设备本地 Cronet，就必须把本项留空。
#    编排是「代理优先」：只要这里填了值且代理可达，**所有平台（含 Lawson）都走代理**，
#    Cronet 那条路径根本不会被调用（src/sources/index.ts 里代理成功后直接 return）。
#    填了值但代理连不上，则每次搜索先白等 3s 连接超时才回落。
#    留空 → searchViaProxy 立刻返回 null → 直接走客户端路径（Android 上即 Cronet）。
#    VITE_ 变量是 build 时编入的，改完必须重新构建安装才生效。
#
# 各平台在「客户端直连」下的现状：
#   - eplus / Pia / LivePocket / TicketDive：CapacitorHttp 直连一直可用。
#   - Lawson(ローチケ)：Akamai 按「TLS 指纹 + HTTP/2 行为 + 头集与 UA 的一致性」判客户端，
#     OkHttp(CapacitorHttp) 会被静默丢弃；Android 改用 Cronet(Chromium 网络栈) 后可本地直取
#     （实测 300-500ms），见 docs/adr/0005-lawson-cronet-on-device.md。
#     iOS 无该原生插件，仍需代理，否则不提供 Lawson。
#
# 什么时候仍然需要填？
#   - iOS 真机要搜 Lawson；
#   - 想让所有平台统一走服务端解析（在 Mac 上调试解析器时方便）。
#
#   - 本地开发：留空即可，dev 浏览器会自动走 vite 代理（/api → 127.0.0.1:8787）。
#   - 真机经 Tailscale 连家里 Mac：填那台 Mac 的 Tailscale 地址，例如
#       VITE_TICKET_PROXY_BASE_URL="http://100.x.y.z:8787"
#     并在 Mac 上常驻运行： HOST=0.0.0.0 npm run server:dev
#     （http 明文白名单见 android/app/src/debug/res/xml/debug_network_security_config.xml）
#
# 注意：.env 已 gitignore，本文件仅作示例；build 时该值会被编入包。
VITE_TICKET_PROXY_BASE_URL=""
